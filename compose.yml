services:
  db:
    image: postgres:15-alpine
    container_name: qr_scanner_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-qr_scanner}
      POSTGRES_USER: ${POSTGRES_USER:-user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - qr-network
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: qr_scanner_backend
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-user}:${POSTGRES_PASSWORD:-password}@db:5432/${POSTGRES_DB:-qr_scanner}
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-change-in-production}
    volumes:
      - uploads_volume:/app/static/uploads
    networks:
      - qr-network
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: qr_scanner_frontend
    depends_on:
      - backend
    networks:
      - qr-network
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: qr_scanner_nginx
    depends_on:
      - backend
      - frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    networks:
      - qr-network
    restart: unless-stopped

networks:
  qr-network:
    driver: bridge

volumes:
  postgres_data:
  uploads_volume: